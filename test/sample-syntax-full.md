# H1: ページタイトル相当（noteではh2に変換される想定）

## H2: 通常の見出し（そのままh2）

### H3: 小見出し（そのままh3）

#### H4: より小さい見出し（太字段落に変換される想定）

##### H5: さらに小さい見出し（太字段落）

###### H6: 最小の見出し（太字段落）

---

## インライン装飾の確認

これは **太字** です。これは *斜体* です。これは ~~取り消し線~~ です。これは `インラインコード` です。

組み合わせも確認します： ***太字斜体***、**太字の中の `コード`**、[リンクの中の **太字** テキスト](https://note.com)。

---

## リストの確認

### 順序なしリスト

- 項目1
- 項目2
  - ネストした項目2-1
  - ネストした項目2-2
- 項目3

### 順序付きリスト

1. ステップ1
2. ステップ2
3. ステップ3

### タスクリスト（GFM）

- [x] 完了済みタスク
- [ ] 未完了タスク
- [ ] あとでやるタスク

---

## 引用の確認

> シンプルな引用文です。
> 複数行の引用も可能です。

> 引用の中で **太字** や `コード`、[リンク](https://note.com) も使えます。

---

## コードブロックの確認

JavaScript:

```javascript
function greet(name) {
  return `Hello, ${name}!`;
}
console.log(greet("note"));
```

Python:

```python
def greet(name):
    return f"Hello, {name}!"

print(greet("note"))
```

言語指定なし:

```
plain text
no syntax highlighting
```

---

## リンクの確認

- 通常リンク: [note.com](https://note.com)
- タイトル付き: [Anthropic](https://www.anthropic.com "Anthropic公式サイト")
- 自動リンク: <https://www.anthropic.com>
- 同一段落内のリンク: 詳細は [こちら](https://note.com) と [こちら](https://www.anthropic.com) を参照。

---

## 画像の確認

公開URLの画像（変換時に「note側で再アップロードしてください」の注釈が入る想定）:

![noteのファビコン](https://note.com/favicon.ico)

別形式（タイトル付き）:

![代替テキスト](https://example.com/image.png "画像のタイトル")

連続して2枚:

![画像A](https://example.com/a.jpg)
![画像B](https://example.com/b.jpg)

---

## 区切り線

下に区切り線が入ります。

---

## エッジケースのテスト

### 連続する空行を含む段落

段落1。

段落2（間に空行）。



段落3（空行が複数）。

### 長文段落の折り返し

これは非常に長い段落のテストです。Markdownでは改行の扱いが処理系によって異なるため、改行尊重（breaks: true）の設定が効いているかを確認します。
この行は前の行から改行されていますが、空行は挟んでいません。
改行が `<br>` に変換されるか、それとも段落内で結合されるかを目視で確認してください。

### インラインコード内の特殊文字

` <script>alert("XSS")</script> ` は、エスケープされて表示されるはずです。

### 終わり

以上で全構文のチェックは完了です。
